{% extends 'base.html.twig' %}

{% block title %}Edit Training{% endblock %}

{% block body %}
    <a href="{{ path('training_show', {'id': training.id}) }}"><i class="fas fa-chevron-left h1 back-btn"></i></a>

    <h1 class='text-center mb-5'>Edit Training</h1>

    {{ include('training/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
