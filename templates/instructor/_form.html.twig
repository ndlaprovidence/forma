{{ form_start(form) }}

    <label>{{'Last name'|trans}}</label>
    {{ form_errors(form.last_name) }}
    {{ form_widget(form.last_name, {'attr': {'class': 'mb-3'}}) }}

    <label>{{'First name'|trans}}</label>
    {{ form_errors(form.first_name) }}
    {{ form_widget(form.first_name, {'attr': {'class': 'mb-5'}}) }}

    <div class='row justify-content-center'>
        <div class='col text-center'>
            <button class="btn btn-lg btn-primary rad-rounded-xs px-5">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}
