{% extends 'base.html.twig' %}

{% block title %}Edit Instructor{% endblock %}

{% block body %}
    <a href="{{ path('instructor_show', {'id': instructor.id}) }}"><i class="fas fa-chevron-left h1 back-btn"></i></a>

    <h1 class='text-center mb-5'>Edit Instructor</h1>

    {{ include('instructor/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('instructor/_delete_form.html.twig') }}
{% endblock %}
