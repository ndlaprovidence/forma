{% extends 'base.html.twig' %}

{% block title %}{{'Edit Instructor'|trans}}{% endblock %}

{% block body %}
    <a href="{{ path('instructor_show', {'id': instructor.id}) }}"><i class="fas fa-chevron-left h1 back-btn"></i></a>

    <h1 class='text-center mb-5'>{{'Edit Instructor'|trans}}</h1>

    {{ include('instructor/_form.html.twig', {'button_label': 'Update'|trans}) }}

    {{ include('instructor/_delete_form.html.twig') }}
{% endblock %}
