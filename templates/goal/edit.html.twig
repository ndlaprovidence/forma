{% extends 'base.html.twig' %}

{% block title %}Edit Goal{% endblock %}

{% block body %}
    <a href="{{ path('goal_show', {'id': goal.id}) }}"><i class="fas fa-chevron-left h1 back-btn"></i></a>

    <h1 class='text-center mb-5'>Edit Goal</h1>

    {{ include('goal/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('goal/_delete_form.html.twig') }}
{% endblock %}
