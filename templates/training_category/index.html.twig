{% extends 'base.html.twig' %}

{% block title %}TrainingCategory index{% endblock %}

{% block body %}
    <h1>TrainingCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for training_category in training_categories %}
            <tr>
                <td>{{ training_category.id }}</td>
                <td>{{ training_category.title }}</td>
                <td>
                    <a href="{{ path('training_category_show', {'id': training_category.id}) }}">show</a>
                    <a href="{{ path('training_category_edit', {'id': training_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('training_category_new') }}">Create new</a>
{% endblock %}
