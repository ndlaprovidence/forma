{% extends 'base.html.twig' %}

{% block title %}Edit Training Category{% endblock %}

{% block body %}
    <a href="{{ path('training_category_show', {'id': training_category.id}) }}"><i class="fas fa-chevron-left h1 back-btn"></i></a>

    <h1 class='text-center mb-5'>Edit Training Category</h1>

    {{ include('training_category/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('training_category/_delete_form.html.twig') }}
{% endblock %}
